function multiply(x, y) {
    return x * y;
}

const multiply = (x, y) => {
    return x * y;
}

const multiply = (x, y) => x * y;

function add(a, b) {
    return a + b;
}

const add = (a, b) => a + b;

const add = (a, b) => {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

const subtract = (a, b) => {
    return a - b;
}

const subtract = (a, b) => a - b;
